name: Production Android Build

# Manual only — run from Expo dashboard or: eas workflow:run production-android.yml
on:
  workflow_dispatch: {}

jobs:
  build_android:
    name: Build Android (production)
    type: build
    params:
      platform: android
      profile: production
