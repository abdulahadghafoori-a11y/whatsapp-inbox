name: Production Android Build

on:
  push:
    branches: ['main']
    paths: ['mobile/**']

jobs:
  build_android:
    name: Build Android (production)
    type: build
    params:
      platform: android
      profile: production
